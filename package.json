{
  "name": "migaki",
  "version": "0.0.0",
  "private": true,
  "description": "Execution optimizer for AI agent workflows: portable execution graphs, provider-aware lowering, and evidence traces.",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json --pretty false && pnpm --filter @migaki/runtime run build && pnpm --filter @migaki/codex run build && pnpm --filter migaki-openai-agents-js run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:e2e": "vitest run --config vitest.e2e.config.ts --passWithNoTests",
    "typecheck": "tsc --noEmit --pretty false && tsc -p tsconfig.test.json --noEmit --pretty false"
  },
  "engines": {
    "node": ">=24.18.0",
    "pnpm": ">=11.9.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.2",
    "eslint": "10.6.0",
    "jiti": "2.7.0",
    "prettier": "3.8.4",
    "typescript": "6.0.3",
    "typescript-eslint": "8.62.0",
    "vitest": "4.1.9"
  }
}
